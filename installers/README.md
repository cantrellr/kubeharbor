# Harbor offline installer staging

The Internet-connected staging script downloads this automatically:

```text
harbor-offline-installer-v2.15.1.tgz
```

Do not use the online installer in the air-gapped environment. The online installer expects to pull images from the Internet and will fail.
